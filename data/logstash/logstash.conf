input {
    jdbc {
        jdbc_driver_library => "/connector/connector.jar"
        jdbc_driver_class => "com.mysql.jdbc.Driver"
        jdbc_connection_string => "jdbc:mysql://mysql:3306/new_schema"
        jdbc_user => "root"
        jdbc_password => "admin"
        jdbc_validate_connection => true
        statement => "SELECT * from new_schema.Posts"
    }
}

output {
    stdout { codec => json_lines }
}
